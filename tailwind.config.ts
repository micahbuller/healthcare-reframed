// Tailwind v4 uses CSS-first configuration via app/globals.css (@theme, @plugin, @import).
// This file is kept for editor tooling compatibility but is not loaded at build time.
import type { Config } from "tailwindcss";

export default {} satisfies Config;
